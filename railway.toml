[build]
builder = "nixpacks"

[deploy]
startCommand = "npx prisma db push --accept-data-loss || true && npm start"
healthcheckPath = "/api/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
